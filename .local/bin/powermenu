#! /bin/sh

chosen=$(printf "  Power Off\n  Restart\n  Log Out\n  Lock" | rofi -dmenu -i -theme-str '@import "power.rasi"')

case "$chosen" in
	"  Power Off") sudo /sbin/poweroff ;;
	"  Restart")sudo /sbin/reboot ;;
	"  Log Out") sudo /sbin/zzz ;;
	"  Lock") slock ;;
	*) exit 1 ;;
esac
