permit persist :wheel
permit nopass :wheel as root cmd /sbin/poweroff
permit nopass :wheel as root cmd /sbin/reboot
permit nopass :wheel as root cmd /sbin/zzz
permit nopass :wheel as root cmd brillo args -A 10 -q
permit nopass :wheel as root cmd brillo args -U 10 -q
